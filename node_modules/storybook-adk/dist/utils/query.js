"use strict";

Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.queryFetch = queryFetch;
exports.querySet = querySet;
function queryFetch(data, api) {
    if (!api || !data) return null;
    var keys = Object.keys(data);
    var fetchedData = {};
    var anyData = false;
    keys.forEach(function (val) {
        var param = api.getQueryParam(val);
        if (param) {
            anyData = true;
            fetchedData[val] = param;
        }
    });
    return anyData && fetchedData;
}

function querySet(data, api) {
    if (!api || !data) return null;
    api.setQueryParams(data);
    return true;
}