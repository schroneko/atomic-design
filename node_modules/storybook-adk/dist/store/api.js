'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _logger = require('../utils/logger');

// eslint-disable-line
var logger = _logger.loggerOff; // note: debug

exports.default = {
    //    incIndex(poddaStore, delta) {
    //        poddaStore.update(state => ({ index: state.index + delta }));
    //    },
    //    setLabel(poddaStore, name, ind) {
    //        poddaStore.update(() => ({ index: ind, label: name }));
    //    },

    $dataInit: function $dataInit(poddaStore, data) {
        logger.log('dataInit:', data);
        poddaStore.set('initData', data);
    },
    setData: function setData(poddaStore, data) {
        poddaStore.update(function () {
            return { data: data };
        });
        logger.log('Set Data:', poddaStore.getAll());
    },
    debugData: function debugData(poddaStore) {
        logger.log('Addon Store:', poddaStore.getAll());
    },
    $queryFetch: function $queryFetch(poddaStore, queryInitData) {
        return {
            queryData: queryInitData
        };
    }
};